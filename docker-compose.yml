services:
  pencarimovie:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pencarimovie-server
    restart: unless-stopped
    ports:
      - "8088:8088"
    environment:
      - PORT=8088
    volumes:
      - ./storage:/app/storage
