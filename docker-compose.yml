services:
  pencarimovie:
    image: pencarimovie-server:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pencarimovie-server
    restart: unless-stopped
    ports:
      - "${PORT:-8088}:8088"
    environment:
      - PORT=${PORT:-8088}
      - FRANKENPHP_NUM_THREADS=${FRANKENPHP_NUM_THREADS:-16}
      - FRANKENPHP_MAX_THREADS=${FRANKENPHP_MAX_THREADS:-64}
      - MALLOC_ARENA_MAX=2
    deploy:
      resources:
        limits:
          memory: 2G
    volumes:
      - ./storage:/app/storage
